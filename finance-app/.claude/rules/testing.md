# Test Agent Rules
- Write to src/test/java ONLY
- NEVER edit src/main/java
- NEVER run the application, only mvn test
- If a test requires a class that doesn't exist, create a stub interface — do not implement it